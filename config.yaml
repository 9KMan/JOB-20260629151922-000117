targets:
  - name: example_directory
    url: https://example.com/listings
    schedule: "0 9 * * *"          # daily at 09:00 UTC
    selector_map:
      row: ".item"
      name: "h2.title"
      phone: ".tel"
      external_id: "[data-id]"
